There are two python script files: problemA.py and problemB.py

The sample inputs are provided inside the scripts. If needed, they can be changed manually.
To run the scripts simply run them via terminal or, alternatively, copy and paste the code in an online python compiler editor.
